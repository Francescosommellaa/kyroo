import styles from "./ModuleCanvas.module.scss";

const ModuleCanvas = () => {
  return (
    <main className={styles.canvas}>
      <h2>Canvas Area</h2>
      {/* Qui andrà il contenuto dei moduli drag-and-drop */}
    </main>
  );
};

export default ModuleCanvas;

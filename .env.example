# Variabili d'ambiente per KYROO
# Copia questo file in .env.local per sviluppo locale

# Frontend Environment Variables (VITE_ prefix for client-side access)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Backend Environment Variables (for Netlify Functions)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Google OAuth Configuration (for Supabase Auth)
# Ottieni queste credenziali da Google Cloud Console:
# 1. Vai su https://console.cloud.google.com/
# 2. Crea un nuovo progetto o seleziona uno esistente
# 3. Abilita Google+ API
# 4. Vai su "Credenziali" → "Crea credenziali" → "ID client OAuth 2.0"
# 5. Configura l'URL di callback: https://your-project-id.supabase.co/auth/v1/callback
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Milvus/Zilliz Configuration
# Frontend Environment Variables (VITE_ prefix for client-side access)
VITE_MILVUS_ENDPOINT=https://in03-your-cluster-id.api.gcp-us-west1.zillizcloud.com

# Backend Environment Variables (for Netlify Functions)
# NOTA: Il token Milvus è utilizzato solo nel backend per sicurezza
MILVUS_ENDPOINT=https://in03-your-cluster-id.api.gcp-us-west1.zillizcloud.com
MILVUS_TOKEN=your-zilliz-api-key-here

# Per produzione, configura queste variabili in:
# - Netlify: Site Settings → Environment Variables
# - Supabase: Project Settings → API

# Nota: Le variabili SUPABASE_URL e SUPABASE_SERVICE_ROLE_KEY sono necessarie
# per il corretto funzionamento delle funzioni Netlify admin